const nums = [9, 2, 3, 6, 7];
// - 排序   冒泡O(n^2)  快排O(nlogn)

function sortArr(arr){
    // const res = arr     // 同一地址 引用时赋值
    return res.concat.sort()
}
console.log(sortArr(nums), nums);

// nums.sort()     //会将原数组一起打乱顺序 这个函数不单纯，有副作用，不可靠的函数
// console.log(nums);