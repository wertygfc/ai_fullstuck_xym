<!--模板（模板是写好的页面，且绑定好了数据，模板不是静态页面）-->
<swiper class="section hero white" indicator-dots="{{true}}">
<bolck wx:for="{{slides}}" wx:key="id">
  <swiper-item>
    <image src="{{item.image}}"/>
    <view class="content centered">
      <view class="sub-header">
      {{item.sub_header}}
      </view>
      <view class="header">
      {{itm.description}}
      </view>
      <view class="description">
      全尺寸七座SUV，接受预定
      </view>
    </view>
  </swiper-item>
</bolck>
</swiper>